# T211 Build-time Rule Validator 验证记录

## 1. 基本信息

- 验证日期：2026-08-11
- Milestone：M2 — Rule System
- Batch：Batch 06B
- Task：T211
- 分支：`m2-rule-system`
- 验证基准提交：`191a20722426bbeef82fe95abecf98d2cc791401`
- Active Requirements Baseline：`docs/product/requirements-baseline-v1.2.md`
- Rule Spec：`docs/rules/common-simple/rule-spec-v1.0.md`
- 验证环境：Windows / PowerShell / Node.js `v24.12.0` / npm `11.6.2`

> 本记录验证当前未提交工作树相对于上述基准提交的 Batch 06B 变更；未执行 commit 或 push。

## 2. 构建阻断链

生产构建在 Vite `buildStart` 阶段执行内置规则校验：

```text
Rule Spec Markdown
        +
unknown RulePackage / Rule Corpus
        ↓
Schema / ID / relation / source / capability / content hash
        ↓
81 / 78 / 3、结构、计分、来源、Rule Version 一致性
        ↓
PASS 才继续生成生产产物
```

规则包仅包含可由严格 Schema 接受的 JSON 数据；构建插件读取本地正式 Rule Spec，不下载或执行远程规则代码。

## 3. 非法 Fixture 阻断结果

| Fixture | 预期阻断 | 实际结果 |
|---|---|---|
| 重复 Pattern ID | `RULE_PACKAGE_INVALID` | PASS — 构建校验拒绝 |
| relation 引用未知 Pattern | `RELATION_INVALID` | PASS — 构建校验拒绝 |
| Pattern 引用未知 Source | `SOURCE_REFERENCE_INVALID` | PASS — 构建校验拒绝 |
| 缺失可信 Capability | `CAPABILITY_INVALID` | PASS — 构建校验拒绝 |
| 启用数量由 78 漂移为 77 | `ENABLED_PATTERN_COUNT_DRIFT` | PASS — 即使重算合法 content hash 仍拒绝 |
| 禁用数量由 3 漂移为 2 | `DISABLED_PATTERN_COUNT_DRIFT` | PASS — 即使重算合法 content hash 仍拒绝 |
| Rule Spec Version 漂移为 `1.0.1` | `RULE_VERSION_DRIFT` | PASS — 构建校验拒绝 |
| Rule Corpus Version 未同步 | `CORPUS_RULE_VERSION_DRIFT` | PASS — 构建校验拒绝 |
| RuleSource 目录缺项 | `SOURCE_DRIFT` | PASS — 构建校验拒绝 |
| Rule Corpus Rule ID 漂移 | `CORPUS_RULE_ID_DRIFT` | PASS — 构建校验拒绝 |

自动化测试源：

- [`build-time-rule-validator.test.ts`](../../../src/infrastructure/rule-validation/build-time-rule-validator.test.ts)
- [`rule-package.test.ts`](../../../src/content/rules/common-simple/rule-package.test.ts)
- [`built-in-rule-repository.test.ts`](../../../src/infrastructure/rule-repository/built-in-rule-repository.test.ts)

## 4. Build 日志

构建命令与输出摘要见 [`T211-build-log.md`](./T211-build-log.md)。关键结果：

```text
[plugin common-simple-rule-validation] Rule validation PASS: common-simple@1.0.0 (81/78/3, 6 sources)
✓ built
```

## 5. 结论

```text
T211 = PASS / DONE
```

内置规则损坏或 Rule Spec 关键事实漂移会在生产构建阶段阻断。当前仅建立 Rule Corpus 的 Rule ID / Rule Version 与引用骨架；78 个启用番型的正式正反例、Recognizer 和完整关系回归仍按计划由 M4 完成。

## 6. 2026-08-12 明杠策略非法 Fixture 增量验证

新增 fixture 将允许 `open-kong` 的 `openKongPolicy.allowedKinds` 清空。严格 Schema 返回 `SCHEMA_INVALID`，Build-time Rule Validator 以 `RULE_PACKAGE_INVALID` 阻断打包；有效的 `common-simple@1.0.0` 仍通过构建期校验。完整命令与结果见 [`open-kong-policy-contract.md`](./open-kong-policy-contract.md)。
